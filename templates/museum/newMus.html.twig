{% extends 'base.html.twig' %}

{% block body %}

    <h2>Création d’un nouveau musée</h2>

    {{ form_start(formMus) }}
        {{ form_widget(formMus) }}
        <div class="form-group row">
            <div class="col-sm-2">

            </div>
            <div class="col-sm-10">
                <button type="submit" class="btn-success btn">Créer</button>
            </div>
        </div>
    {{ form_end(formMus) }}

{% endblock %}